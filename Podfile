platform :ios, '9.0'

target 'ConfigProject' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Utils
  pod 'SwiftLint', '0.27.0'
  pod 'Fabric'
  pod 'Crashlytics'
  pod 'SwifterSwift', '4.6.0'
end
